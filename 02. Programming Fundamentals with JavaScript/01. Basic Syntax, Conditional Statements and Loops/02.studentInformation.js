function studentInformation(studentName, studentAge, averageGrade) {
    let text = `Name: ${studentName}, Age: ${studentAge}, Grade: ${averageGrade.toFixed(2)}`;

    console.log(text);
}

studentInformation('John', 15, 5.54678);